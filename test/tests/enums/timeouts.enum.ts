/**
 * Enum representing different timeout values in milliseconds.
 */
export enum Timeout {
    NO_WAIT = 0,
    BUTTON = 5000,
    SPINNER = 30000,
    INPUT = 2000,
    TABLE_CONTAINER = 20000,
    VERIFY_MODAL = 20000,
    TAB = 2000,
    PAGE = 20000,
    ERROR_MESSAGE = 5000,
    ONE_MINUTE = 60000,
    WAIT_FOR_LOGIN = 30000,
    DB_QUERY = 90000,
    ACTION_TIMEOUT = 30000,
    CHECKBOX_ACTION = 1000,
    SPINNER_TWO_SECONDS = 2000
}
