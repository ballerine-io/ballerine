apiVersion: v1
kind: Service
metadata:
  name: {{ include "wf-service.fullname" . }}
  labels:
    {{- include "wf-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wf-service.selectorLabels" . | nindent 4 }}
