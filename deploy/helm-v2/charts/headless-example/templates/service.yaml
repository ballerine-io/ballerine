apiVersion: v1
kind: Service
metadata:
  name: {{ include "headless-example.fullname" . }}
  labels:
    {{- include "headless-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "headless-example.selectorLabels" . | nindent 4 }}
