apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflows-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "workflows-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workflows-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
