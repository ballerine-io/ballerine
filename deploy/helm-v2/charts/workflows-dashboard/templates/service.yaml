apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflows-dashboard.fullname" . }}
  labels:
    {{- include "workflows-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workflows-dashboard.selectorLabels" . | nindent 4 }}
