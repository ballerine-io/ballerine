apiVersion: v1
kind: Secret
metadata:
    name: {{ include "kyb-app.fullname" . }}
    namespace: {{ .Release.Namespace | quote }}
    labels:
      app: {{ include "kyb-app.fullname" . }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.applicationConfig }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}