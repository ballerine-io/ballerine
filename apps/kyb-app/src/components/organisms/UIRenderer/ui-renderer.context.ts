import { UIRendererContext } from '@app/components/organisms/UIRenderer/types/context';
import { createContext } from 'react';

export const uiRendererContext = createContext({} as UIRendererContext);
