import { PageResolverContext } from '@/components/organisms/DynamicUI/PageResolver/types';
import { createContext } from 'react';

export const pageResolverContext = createContext({} as PageResolverContext);
