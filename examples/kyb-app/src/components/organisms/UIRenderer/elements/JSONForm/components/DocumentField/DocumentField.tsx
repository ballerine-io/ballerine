import { RJSVInputProps } from '@ballerine/ui';
import { useCallback, useState } from 'react';

export const DocumentField = (props: RJSVInputProps) => {
  const [isUploading, setUploading] = useState(false);

  const handleChange = useCallback(() => {}, []);
};
