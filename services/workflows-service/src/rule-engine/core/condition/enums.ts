export enum ConditionType {
  EQUALS = 'EQUALS',
  BETWEEN = 'BETWEEN',
  GT = 'GT',
  LT = 'LT',
  GTE = 'GTE',
  LTE = 'LTE',
  LAST_YEAR = 'LAST_YEAR',
}

export enum ConditionOperator {
  AND = 'and',
  OR = 'or',
}
