/**
 * File size constants — mirrors @ballerine/common consts that are missing from dist.
 * These should be removed once @ballerine/common is rebuilt with the new exports.
 */
export const FILE_MAX_SIZE_IN_KB = 1024;
export const FILE_MAX_SIZE_IN_BYTE = 10 * FILE_MAX_SIZE_IN_KB * 1024; // 10 MB
